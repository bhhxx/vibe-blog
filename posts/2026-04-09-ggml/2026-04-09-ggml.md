---
title: "ggml_tensor 结构体阅读笔记"
date: 2026-04-09
tags: ["AI Infra"]
description: "从 ggml_tensor 结构体入手理解 ggml 的张量表示"
ai_summary: >-
  文章围绕 `ggml_tensor` 结构体展开，记录了 type、buffer、shape/stride、op、src、view、data 和 extra 等字段，试图从运行时数据结构理解 ggml 的张量表示。
ai_summary_topics:
  - ggml tensor 布局
  - shape / stride 元数据
  - view 与 buffer
ai_mastery_signal: >-
  已经开始读真实推理运行时的源码结构，而不是只看高层概念；不过当前主要停留在字段级注释，还未进一步连到计算图执行和内存规划。
ai_adjacent_gap: >-
  下一步最值得补的是这些 tensor 描述符如何驱动图执行、量化 block 布局、backend buffer 管理，以及 CPU/CUDA 后端怎样共享这套抽象。
ai_summary_model: gpt-5.4
ai_summary_updated_at: 2026-04-21
---

## ggml_tensor

```C
// n-dimensional tensor
struct ggml_tensor {
    enum ggml_type type; // 张量的每个元素的类型

    struct ggml_backend_buffer * buffer; // 真实数据存放的 buffer

    int64_t ne[GGML_MAX_DIMS]; // number of elements
    size_t  nb[GGML_MAX_DIMS]; // stride in bytes:
                               // nb[0] = ggml_type_size(type)
                               // nb[1] = nb[0]   * (ne[0] / ggml_blck_size(type)) + padding
                               // nb[i] = nb[i-1] * ne[i-1]

    // compute data
    enum ggml_op op;

    // op params - allocated as int32_t for alignment
    int32_t op_params[GGML_MAX_OP_PARAMS / sizeof(int32_t)];

    int32_t flags;

    struct ggml_tensor * src[GGML_MAX_SRC];

    // source tensor and offset for views
    struct ggml_tensor * view_src;
    size_t               view_offs;

    void * data;

    char name[GGML_MAX_NAME];

    void * extra; // extra things e.g. for ggml-cuda.cu

    char padding[8];
};
```

